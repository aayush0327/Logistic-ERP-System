"use client";

import CompanyAnalyticsPage from "@/app/(protected)/company-admin/analytics/page";

export default function BranchManagerAnalyticsPage() {
  // Reuse the same analytics dashboard for Branch Manager
  // You can customize it later if needed for role-specific views
  return <CompanyAnalyticsPage />;
}
