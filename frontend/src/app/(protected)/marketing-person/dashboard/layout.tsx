export const metadata = {
  title: "Marketing Person - Orders",
  description: "Manage customer orders",
};

export default function MarketingPersonLayout({ children }: { children: React.ReactNode }) {
  return children;
}
